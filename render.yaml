services:
  - type: web
    name: emotion-recognition
    env: node
    plan: free
    region: frankfurt
    branch: main
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: PORT
        value: "10000"
